# SQLite database location. For Coolify, mount a persistent volume at /app/data.
DATABASE_URL="file:../data/todo.db"

# Optional: set this to a public URL for links in future integrations.
NEXT_PUBLIC_APP_URL=""
